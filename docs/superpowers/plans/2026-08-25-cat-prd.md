# /cat-prd 自包含 PRD 生成 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 新 skill `/cat-prd`:读品类分析报告 → 交互确认 3 个决策 → 生成 10 节自包含 PRD,可直接交给任何 AI 编码工具依次实现。

**Architecture:** 纯 skill 文档(零代码),与 `/cat-scan` 同链路:品类报告是唯一数据输入,PRD 是唯一产出。自包含性靠 skill 内的硬约束(禁止外部引用、数字内联)+ 真实 subagent 实测验收。

**Tech Stack:** 无新依赖;skill markdown + 既有 `reports/` 目录。

**设计文档:** `docs/superpowers/specs/2026-08-25-cat-prd-design.md`

**验收基线数据:** PDF 扫描品类报告已存在:`reports/2026-08-25-品类分析-pdf-scanner.md`(10 节结构,含 per-app 功能矩阵、痛点证据、机会点)。

---

### Task 1: 创建 `/cat-prd` skill

**Files:**
- Create: `.claude/skills/cat-prd/SKILL.md`

- [ ] **Step 1: 写 SKILL.md 全文**

```markdown
---
name: cat-prd
description: 基于品类分析报告生成自包含 PRD(产品需求文档),可直接交给任意 AI 编码工具依次实现。用法:/cat-prd <品类slug> [ios|android|cross] [buyout|subscription|free-ad]。触发词:PRD、需求文档、生成需求、需求生成。
---

# 品类报告 → 自包含 PRD

读品类分析报告(`reports/{日期}-品类分析-{slug}.md`),交互确认 3 个关键决策,
生成一份**完全自包含**的 PRD——拿到任何其他项目、任何 AI 编码工具会话,零依赖
即可依次实现。
设计文档:docs/superpowers/specs/2026-08-25-cat-prd-design.md

## 参数解析

品类 slug(必填,定位报告;给的是品类名则模糊匹配最近的报告并复述确认);
平台 `ios|android|cross`(可选,预答交互问题 1);商业模式
`buyout|subscription|free-ad`(可选,预答交互问题 2)。

## 流程

1. **定位报告**:`reports/` 下按 slug 匹配 `*-品类分析-{slug}.md`(多份取最新)。
   - 找不到 → 提示先跑 `/cat-scan {品类}`,停止。
   - 报告快照日期(文件名里的日期)距今 > 90 天 → 提示数据可能过时、建议
     重跑 /cat-scan,不阻塞。
2. **交互 3 问**(命令参数已答的跳过;每问附报告中的证据辅助决策):
   - 目标平台:ios / android / cross
   - 商业模式:buyout(买断)/ subscription(订阅)/ free-ad(免费+广告)
     ——附报告变现证据(如"本品类买断制口碑溢价 4.9 分 vs 订阅墙差评占 30-40%")
   - 主定位:列出报告"机会点"节的内容供选择,另给"AI 推荐"选项
     (推荐时按 痛点强度 × 竞争空白 给出理由)
3. **生成 PRD**:按下方结构写作,落盘 `reports/{今天日期}-PRD-{slug}.md`。
4. **执行摘要**:向用户输出 3-5 句(一句话定位、P0 范围、里程碑数)。

## PRD 结构(10 节,全自包含)

0. **使用说明(给 AI 编码工具)**:本文档按里程碑依次实现;每个功能完成后
   对照其验收标准(AC)自验;技术栈可按团队能力替换,但功能契约(AC)不变。
1. **产品概述**:品类定义与背景(从报告"品类概览"节内联)、一句话定位。
2. **目标用户与核心场景**:从报告推导的细分人群 × 场景(2-4 组)。
3. **定位与差异化主张**:选定机会点展开成主张;**内联**报告的痛点证据
   (评论摘录 + 出现频率)论证"为什么是我"。
4. **竞品基准表**:头部 3-5 家 × 列(主要功能/特色功能/价格/评分/下载量),
   **从报告的竞争格局/功能矩阵节内联复制数字**,表下标注数据快照日期。
5. **功能规格**(主体,三层):
   - **P0 品类标配基线**:来自报告"标配功能"清单——缺了就不算该品类;
   - **P1 差异化主打**:直接支撑第 3 节定位的功能;
   - **P2 亮点增强**:锦上添花。
   每个功能块:ID(F-P0-01 / F-P1-01 / …)/名称/描述(1-3 句)/
   验收标准(1-4 条,可测试表述)/依赖(其他功能 ID 或"无")。
   AC 写法:描述可观察、可验证的行为("扫描 10 页导出单个 PDF 且页序正确" ✓;
   "体验流畅" ✗)。写不出 AC 的功能,回头重想描述。
6. **非功能需求**:从痛点推导(性能/隐私/离线/无广告等),每条同样可测表述。
7. **变现设计**:选定模式的具体方案(定价档/免费额度/触发付费的动作);
   显式列出"规避的竞品痛点"(如不设水印陷阱、不搞买断转订阅)。
8. **里程碑与实现顺序**:P0→P1→P2 各为一个里程碑;P0 内部按技术依赖排序
   (基础能力在前,如 相机捕获→边缘检测→导出,不倒序);每里程碑给
   "完成定义"(全部 AC 通过 = 完成)。
9. **附录**:名词表;数据来源声明(基于 {快照日期} 的品类快照,数字来自
   当期双平台应用商店公开数据)。

## 质量约束

- **自包含硬约束**:全文禁止"见品类报告 X 节"式引用;一切证据/表格/数字内联
- **数字不臆造**:竞品评分/下载量/价格只从品类报告抄录
- **全程中文**;技术名词保留英文

## 错误处理

- 报告缺"机会点"节(格式不符/旧版)→ 降级:让用户口述主定位,再继续
- 读报告失败等其它错误 → 报告错误并停止
```

- [ ] **Step 2: 验证 skill 注册**

新 skill 应出现在可用 skill 列表(harness 会即时识别 `.claude/skills/` 下新目录);
同时人工核对:frontmatter 两个 `---` 配对、name 与目录名一致、markdown 代码块围栏完整。

- [ ] **Step 3: 提交**

```bash
git add .claude/skills/cat-prd/SKILL.md
git commit -m "feat: /cat-prd 品类报告转自包含 PRD skill"
```

---

### Task 2: README 增补

**Files:**
- Modify: `README.md`(品类分析小节)

- [ ] **Step 1: 在 README `## 品类分析` 小节的 `/cat-scan` 示例之后追加两行**

在 `## 品类分析` 小节内、`# 只抓品类样本数据(不分析)` 注释行之前插入:

```markdown
    # 从品类报告生成自包含 PRD(交给任意 AI 编码工具依次实现)
    /cat-prd pdf-scanner cross buyout
```

- [ ] **Step 2: 通读 README 确认结构未破坏,提交**

```bash
git add README.md
git commit -m "docs: /cat-prd 使用说明"
```

---

### Task 3: 端到端验收(真实生成 + 自包含实测)

**Files:**
- Create: `reports/2026-08-25-PRD-pdf-scanner.md`(运行产出)

- [ ] **Step 1: 按 SKILL.md 真实生成 PRD**

执行 `/cat-prd pdf-scanner cross buyout`(平台与商业模式由参数预答;主定位按
"AI 推荐"走:依据报告机会点节,按痛点强度 × 竞争空白推荐并给理由)。
产出 `reports/2026-08-25-PRD-pdf-scanner.md`。

- [ ] **Step 2: 人工核对清单**

逐项核对 PRD:
- [ ] 10 节齐全(0-9),编号正确
- [ ] 第 4 节竞品基准表含 3-5 家、每家有数字(评分/下载量/价格)+ 表下快照日期
- [ ] 第 5 节三层分明:P0 来自报告标配清单;P1 支撑定位;功能有
      ID/描述/AC/依赖四要素
- [ ] 抽检 5 条 AC:全部可测试(可观察行为表述,无"流畅/好用"类虚词)
- [ ] 全文无"见报告/见品类分析"式外部引用(grep 验证:
      `grep -nE "见(品类)?报告|见.*分析报告" reports/2026-08-25-PRD-pdf-scanner.md` 应无输出)
- [ ] 第 8 节 P0 顺序符合技术依赖(基础能力在前)

- [ ] **Step 3: 自包含性实测(关键验收)**

派一个全新 subagent,**只给它 PRD 文件路径**,指令:
"你是 AI 编码工具。读这份 PRD,仅凭文档内容排出 P0 里程碑的实现顺序
(任务列表),并列出文档中缺失、无法开始实现的信息(如有)。不要问任何问题。"
成功标准:subagent 无需追问即排出完整 P0 顺序;若它列出"缺失信息",逐条评估
——属实则回到 Step 1 修补 PRD 再测。

- [ ] **Step 4: 提交 PRD**

```bash
git add "reports/2026-08-25-PRD-pdf-scanner.md"
git commit -m "docs: PDF 扫描品类 PRD(/cat-prd 验收产物)"
```

---

## 自审记录

- Spec 覆盖:§2 命令形态(Task 1 SKILL.md 参数解析)、§3 四步流程(Task 1 流程节)、§4 十节结构(Task 1 PRD 结构节)、§5 质量约束(Task 1 质量约束节)、§6 错误处理(Task 1 错误处理节)、§7 验收(Task 3 Step 2/3 对应 spec 的两条验收)、README(Task 2)——全覆盖
- 占位符:无 TBD/TODO;SKILL.md 全文完整给出
- 一致性:命令参数 `[ios|android|cross] [buyout|subscription|free-ad]` 与交互 3 问、spec §2 一致;PRD 落盘路径 skill 内两处一致;验收命令的 slug 与已有报告文件名(pdf-scanner)一致
